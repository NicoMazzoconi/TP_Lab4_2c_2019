import { NombreMayusculasPipe } from './nombre-mayusculas.pipe';

describe('NombreMayusculasPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreMayusculasPipe();
    expect(pipe).toBeTruthy();
  });
});
