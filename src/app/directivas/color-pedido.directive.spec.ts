import { ColorPedidoDirective } from './color-pedido.directive';

describe('ColorPedidoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorPedidoDirective();
    expect(directive).toBeTruthy();
  });
});
